<div class="page-header">
  <h1>
    All articles
    <%= link_to "Create new article", new_article_path, class: "btn btn-default new_article" %>
</h1>
  <div class="">

  </div>

</div>

<div class="list-group" id="articles">
  <% @articles.each do |article| %>

      <%= link_to article.title, article_path(article), class: 'list-group-item' %>
    
  <% end %>
</div>
